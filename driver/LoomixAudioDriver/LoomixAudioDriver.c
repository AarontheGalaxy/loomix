#include "LoomixAudioDriver.h"

/*
 * Placeholder translation unit proving the build, signing and static-check
 * pipeline before the AudioServerPlugIn entry point, factory function and
 * Info.plist land in M1 (spec section 3.4).
 */
const char *const kLoomixAudioDriverVersion = "0.0.0-skeleton";
