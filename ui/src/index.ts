// Proves the TypeScript build, lint and test pipeline before the Tauri +
// React front end lands with the first milestone that needs a UI surface.
export function ping(): "pong" {
  return "pong";
}
